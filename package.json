{
  "name": "crm-for",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "cd whatsapp-crm && npm run build",
    "dev": "cd whatsapp-crm && npm run dev",
    "start": "cd whatsapp-crm && npm start",
    "migrate": "tsx scripts/migrate-supabase.ts",
    "db:setup": "tsx scripts/setup-database.ts",
    "generate:posts": "tsx scripts/generate-shakumaku-posts.ts"
  },
  "dependencies": {
    "@google/generative-ai": "^0.21.0",
    "@supabase/supabase-js": "^2.102.1",
    "@types/cors": "^2.8.19",
    "cors": "^2.8.6",
    "dotenv": "^16.6.1"
  },
  "devDependencies": {
    "@types/commander": "^2.12.0",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
